{
  "name": "meu-projeto",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "scripts": {
    "start": "concurrently \"npm start --workspace backend\" \"npm start --workspace frontend\"",
    "dev": "npm run start",
    "install-all": "npm install"
  }
}
